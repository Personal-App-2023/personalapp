server:
  port: 9094

spring:
  profiles:
    active: dev
  application:
    name: THOUGHTS-SERVICE
  data:
    mongodb:
      uri: mongodb://localhost:27017/thoughtsdb
      host: localhost
      port: 27017
      database: thoughtsdb
      repositories:
        enabled: true
